/**
 * Canonical realtime layer adapter — chat subscriptions / typing / presence.
 * Implementation: src/hooks/use-chat-realtime.ts (single client wrapper).
 */

export { useChatRealtime } from "@/hooks/use-chat-realtime";

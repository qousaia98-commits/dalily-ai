# LEGACY (directory search UI remnants)

Sprint 10 removed the `/search` product route (redirect → `/request/new`).

Kept in this folder because still referenced live:
- `search-hero.tsx` / `search-form.tsx` (+ voice/vision/diagnosis) — marketing homepage + `/ai`
- `category-grid.tsx` — marketing homepage
- `match-reasons-list.tsx` / `recommendation-badges-list.tsx` — provider cards
- `voice-waveform.tsx` — messaging voice recorder

Do not reintroduce directory browse as the primary customer spine.
